#!/usr/bin/env ruby

require_relative '../app/machine'

opts = {}
if file = ARGV.shift
  opts[:program_file] = file
end
if file = ARGV.shift
  opts[:input_file] = file
end
$m = Machine.new(opts)
$m.run
