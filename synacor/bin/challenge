#!/usr/bin/env ruby

require_relative '../app/machine'

opts = {}
if ARGV[0] == '-d'
  opts[:debug] = true
  ARGV.shift
end
if file = ARGV.shift
  opts[:program_file] = file
end
if file = ARGV.shift
  opts[:input_file] = file
end
$m = Machine.new(opts)
$m.run
